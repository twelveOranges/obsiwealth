{
  "name": "obsiwealth",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "release": "node build.js && node scripts/make-release.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "obsidian": "^1.4.0",
    "typescript": "^5.9.3"
  }
}
